# 项目代码介绍

本文档单独列出本项目中最主要的代码文件，并说明它们在整体流程中的作用。

## 1. 计算核心

### `MDP_2/Services/LevelingAdjustmentService.cs`
- **作用**：项目的核心算法服务，负责完成水准测量数据的主要计算流程。
- **关键入口**：`Compute(List<LPointClass> points, List<LineClass> lines)`。
- **主要工作**：
  1. 检查输入数据是否完整（点和边不能为空、已知点数量是否足够）。
  2. 估算未知点初始高程（`EstimateInitialHeights`）。
  3. 构建从起点到终点的测线顺序（`BuildRoute`）。
  4. 计算高差闭合差、限差，并按距离配赋改正数。
  5. 计算改正后高差和各点平差高程。
  6. 生成结果文本输出（`BuildResultText`）。
- **为什么重要**：这是“数据 -> 结果”的核心枢纽，所有最终数值都从这里产生。

## 2. 数据读取核心

### `MDP_2/Services/CsvDataLoader.cs`
- **作用**：将 CSV 文本解析成程序可计算的点集合与边集合。
- **关键入口**：`Parse(string text)`。
- **主要规则**：
  - 使用 `[Points]` 与 `[Edges]` 两段格式。
  - 点数据映射到 `LPointClass`，边数据映射到 `LineClass`。
  - 提供 `GetEmbeddedExampleCsv()` 便于快速加载示例。
- **为什么重要**：它决定输入数据能否正确进入计算流程，是算法前置环节。

## 3. 数据结构核心

### `MDP_2/Models/LPointClass.cs`
- **作用**：定义“点”的数据结构。
- **关键字段**：
  - `PID`：点名。
  - `H`：点高程（已知值或当前值）。
  - `InitialH`：初始高程。
  - `AdjustedH`：平差后高程。
  - `IsControlP`、`IsH0`、`IsCommonP`：点属性标志。
- **技术特点**：实现 `INotifyPropertyChanged`，支持界面绑定后自动刷新。
- **为什么重要**：点数据是整个测量计算的基础对象。

### `MDP_2/Models/LineClass.cs`
- **作用**：定义“观测边（测段）”的数据结构。
- **关键字段**：
  - `LID`、`SPID`、`EPID`：边号与起终点。
  - `ForwardDH`、`BackwardDH`：往返测高差。
  - `Distance`：测段距离。
- **关键计算属性**：
  - `dH`：均值高差。
  - `Di`、`DiMm`、`Di2Mm2`、`PDi2Mm2PerKm`：精度评价相关指标。
  - `Correction`、`CorrectedDH`：改正数与改正后高差。
- **为什么重要**：承载测量观测值与精度指标，是平差计算的直接输入。

## 4. 界面与交互核心

### `MDP_2/MainWindow.xaml.cs`
- **作用**：主窗口后台逻辑，连接“界面操作、数据加载、计算服务、结果展示”。
- **关键流程**：
  - 加载示例：`LoadSampleButton_OnClick`。
  - 导入 CSV：`ImportCsvButton_OnClick`。
  - 执行计算：`ComputeButton_OnClick`（调用 `LevelingAdjustmentService.Compute`）。
  - 刷新集合与当前点表格：`RefreshCollection`、`RefreshCurrentPoints`。
  - 绘制剖面简图：`DrawProfile`。
- **为什么重要**：它是用户操作的入口，负责把各模块串起来。

### `MDP_2/MainWindow.xaml`
- **作用**：定义主界面布局与控件。
- **主要区域**：
  - 点要素窗口（点表）。
  - 边要素窗口（边表）。
  - 当前点窗口（初值查看）。
  - 结果文本输出窗口。
  - 图形简图区域。
- **为什么重要**：决定数据如何被用户查看与操作。

## 5. 参考与入口文件（辅助）

### `sample_example315.csv`
- **作用**：提供示例输入数据，方便快速测试计算流程。

### `README.md`
- **作用**：项目说明文档，包含运行方式、CSV格式和类说明。

### `MDP_2/MDP_2.csproj`
- **作用**：项目构建配置（目标框架、WPF开关、依赖管理等）。

## 6. 一句话理解项目主链路

用户在界面导入数据（`CsvDataLoader`） -> 数据进入模型（`LPointClass`、`LineClass`） -> 调用算法服务计算（`LevelingAdjustmentService`） -> 在主窗口显示表格、文本和简图（`MainWindow.xaml/.cs`）。

